apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  type: ClusterIP  # Cambia a LoadBalancer si necesitas acceso externo.
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres

